edition = "2024"
reorder_imports = true
reorder_modules = true
max_width = 100
use_small_heuristics = "Default"

# group_imports = "StdExternalCrate" and imports_granularity = "Crate"
# are unstable and require nightly rustfmt. Semantic protection against
# wildcard imports is enforced by clippy's `wildcard_imports = "deny"`
# lint in Cargo.toml. Style grouping is manual for now.

